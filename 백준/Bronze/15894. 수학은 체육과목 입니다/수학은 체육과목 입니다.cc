#include<bits/stdc++.h>
#define DEBUG true
#define FAST_IO 
using namespace std;

typedef long long ll;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);

    ll n; cin >> n;

    cout << 4 * n;

}